# 🔍 membershipPrice: 0 - Root Cause Flow Diagram

## Case 1: MEMBER with membershipPrice: 0 (BUG!)

```
┌─────────────────────────────────────────────────────────────┐
│  User adds "Blood Coagulation" to cart                      │
│  User Type: MEMBER (has active membership)                  │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│  Backend: Fetch item from database                          │
│  Query: SELECT * FROM items WHERE id = 'blood-coag-id'      │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│  ❌ PROBLEM: Price field is NULL or 0 in database          │
│  Result: item.price = 0 or NULL                             │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│  Backend: Build cart response                               │
│  cartItem.original_price = item.price ?? 0  // Sets to 0    │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│  Backend: Calculate membership price                        │
│  if (user.isMember) {                                       │
│    cartItem.membershipPrice = original_price * 0.90         │
│    cartItem.membershipPrice = 0 * 0.90 = 0  ❌              │
│  }                                                           │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│  API Response:                                              │
│  {                                                          │
│    "test_name": "Blood Coagulation",                       │
│    "quantity": 1,                                          │
│    "original_price": 0,        ❌ BUG                      │
│    "membershipPrice": 0        ❌ CASCADING BUG            │
│  }                                                          │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│  Test Code: Extract values                                  │
│  membershipPriceObj = response.get("[0].membershipPrice")   │
│  Result: 0                                                  │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│  Console Output (NEW LOGGING):                              │
│                                                              │
│  🔍 ====== WHY IS membershipPrice ZERO? ======             │
│     📍 Item: Blood Coagulation                              │
│     📍 membershipPrice from API: ₹0                         │
│     📍 original_price from API: ₹0                          │
│     📍 Expected: ₹0 (can't calculate, no original price)    │
│                                                              │
│     🔍 ROOT CAUSE: original_price is ZERO!                  │
│     ⚠️  When original_price = 0, backend returns            │
│         membershipPrice = 0                                 │
│     ⚠️  This is a CASCADING BUG - fix original_price!       │
│  ============================================                │
└─────────────────────────────────────────────────────────────┘
```

---

## Case 2: NON-MEMBER with membershipPrice: 0 (EXPECTED)

```
┌─────────────────────────────────────────────────────────────┐
│  User adds "CBC Test" to cart                               │
│  User Type: NEW_USER (no membership)                        │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│  Backend: Fetch item from database                          │
│  Query: SELECT * FROM items WHERE id = 'cbc-test-id'        │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│  ✅ OK: Price found in database                             │
│  Result: item.price = 310                                   │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│  Backend: Build cart response                               │
│  cartItem.original_price = item.price = 310  ✅             │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│  Backend: Check membership                                  │
│  if (user.isMember) {                                       │
│    // User is NOT a member, skip this                      │
│  } else {                                                   │
│    cartItem.membershipPrice = 0  ✅ EXPECTED                │
│  }                                                           │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│  API Response:                                              │
│  {                                                          │
│    "test_name": "CBC Test",                                │
│    "quantity": 1,                                          │
│    "original_price": 310,      ✅ CORRECT                  │
│    "membershipPrice": 0        ✅ CORRECT (non-member)     │
│  }                                                          │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│  Test Code: Extract values                                  │
│  membershipPriceObj = response.get("[0].membershipPrice")   │
│  Result: 0                                                  │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│  Console Output (NEW LOGGING):                              │
│                                                              │
│  🔍 ====== NON-MEMBER membershipPrice Check ======         │
│     📍 Item: CBC Test                                       │
│     📍 User Type: NEW_USER (NOT a member)                   │
│     📍 membershipPrice from API: ₹0                         │
│                                                              │
│     ✅ membershipPrice is 0 for non-member - EXPECTED       │
│     ℹ️  Non-members don't get membership discount          │
│  ================================================            │
└─────────────────────────────────────────────────────────────┘
```

---

## Case 3: MEMBER with original_price but membershipPrice: 0 (RARE BUG)

```
┌─────────────────────────────────────────────────────────────┐
│  User adds "Test Item" to cart                              │
│  User Type: EXISTING_MEMBER (has membership)                │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│  Backend: Fetch item - price exists (₹310)                  │
│  cartItem.original_price = 310  ✅                          │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│  Backend: Calculate membership price                        │
│  if (user.isMember) {                                       │
│    ❌ BUG: Logic error in backend                          │
│    cartItem.membershipPrice = 0  (WRONG!)                   │
│    // Should be: original_price * 0.90 = 279               │
│  }                                                           │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│  API Response:                                              │
│  {                                                          │
│    "original_price": 310,      ✅ CORRECT                  │
│    "membershipPrice": 0        ❌ BUG (should be 279)      │
│  }                                                          │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│  Console Output (NEW LOGGING):                              │
│                                                              │
│  🔍 ====== WHY IS membershipPrice ZERO? ======             │
│     📍 membershipPrice from API: ₹0                         │
│     📍 original_price from API: ₹310                        │
│     📍 Expected: ₹279                                       │
│                                                              │
│     🔍 ANOMALY: original_price is NOT zero but              │
│        membershipPrice is ZERO!                             │
│     ⚠️  Backend is NOT calculating:                         │
│         membershipPrice = original_price * 0.90             │
│     ⚠️  Backend is setting membershipPrice = 0              │
│  ============================================                │
└─────────────────────────────────────────────────────────────┘
```

---

## Quick Decision Tree

```
                    Is membershipPrice = 0?
                            │
                            ▼
                          ┌───┐
                          │YES│
                          └─┬─┘
                            │
                ┌───────────┴───────────┐
                ▼                       ▼
          Is user a MEMBER?       Is user NON-MEMBER?
                │                       │
                ▼                       ▼
              ┌───┐                   ┌───┐
              │YES│                   │YES│
              └─┬─┘                   └─┬─┘
                │                       │
                ▼                       ▼
    Is original_price = 0?       ✅ EXPECTED
                │                 (Non-members
        ┌───────┴───────┐         get no discount)
        ▼               ▼
      ┌───┐           ┌───┐
      │YES│           │NO │
      └─┬─┘           └─┬─┘
        │               │
        ▼               ▼
    ❌ CASCADING     ❌ CALCULATION
       BUG              BUG
    (Fix price       (Backend not
     first)           applying
                      discount)
```

---

## Summary: Where membershipPrice: 0 Comes From

| Scenario | User | original_price | membershipPrice | Source | Console Shows |
|----------|------|----------------|-----------------|--------|---------------|
| 1 | MEMBER | 0 | 0 | `data.items[i].membershipPrice` | `🔍 ROOT CAUSE: original_price is ZERO!` |
| 2 | NON-MEMBER | 310 | 0 | `data.items[i].membershipPrice` | `✅ membershipPrice is 0 for non-member - EXPECTED` |
| 3 | MEMBER | 310 | 0 | `data.items[i].membershipPrice` | `🔍 ANOMALY: original_price NOT zero but membershipPrice is ZERO!` |

**In all cases, the value comes from the API response at the same JSON path, but the REASON is different!**

The console logging now explains the ROOT CAUSE for each scenario. ✅
